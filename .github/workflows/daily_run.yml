name: Supabase Cron Job

on:
  # 1. ตั้งเวลา (UTC Time) - เลข 0 1 * * * แปลว่า ตี 1 UTC (ประมาณ 8 โมงเช้าไทย)
  schedule:
    on:
  schedule:
    # -------------------------------------------------------
    # 🕛 รอบเที่ยงตรง (12:00 น.) รันทุกวัน (จันทร์-อาทิตย์)
    # 12:00 - 7 = 05:00 UTC
    # -------------------------------------------------------
    - cron: '0 5 * * *'

    # -------------------------------------------------------
    # 🏢 วันจันทร์ - ศุกร์ (1-5)
    # -------------------------------------------------------
    # 08:30 น. -> 01:30 UTC
    - cron: '30 1 * * 1-5'
    # 16:45 น. -> 09:45 UTC
    - cron: '45 9 * * 1-5'

    # -------------------------------------------------------
    # 🏖️ วันเสาร์ (6)
    # -------------------------------------------------------
    # 09:30 น. -> 02:30 UTC
    - cron: '30 2 * * 6'
    # 17:45 น. -> 10:45 UTC
    - cron: '45 10 * * 6'

    # -------------------------------------------------------
    # ☀️ วันอาทิตย์ (0)
    # -------------------------------------------------------
    # 10:30 น. -> 03:30 UTC
    - cron: '30 3 * * 0'
    # 18:45 น. -> 11:45 UTC
    - cron: '45 11 * * 0'

  workflow_dispatch:
  
  # 2. ปุ่มกดมือ (เผื่ออยากกดสั่งรันเดี๋ยวนี้)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ดึงโค้ดเรามา
      - name: Checkout code
        uses: actions/checkout@v3

      # ติดตั้ง Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      # ติดตั้ง Library (supabase)
      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      # รันไฟล์ Python พร้อมส่ง Key เข้าไป
      - name: Run script
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          # ต้องมี 2 บรรทัดนี้เพิ่มขึ้นมาครับ 👇
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}
        run: python main.py
